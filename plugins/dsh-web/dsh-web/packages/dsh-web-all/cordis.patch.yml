# AUTO-GENERATED by scripts/aggregate.mjs — do not edit
# This file is derived from the aggregate.yml manifest in this package;
# edit aggregate.yml and rerun `node scripts/aggregate.mjs`.
# Rows are namespaced web-ui-* so the bundle can coexist with standalone installs.

# from self
- insert:
    - id: web-ui-compat
      name: '@linxin666/dsh-web-all'

# from ../dsh-web-settings
- insert:
    - id: web-ui-settings
      name: '@linxin666/dsh-client-ui-web-ui-settings'

# from ../dsh-plugin-manager
- insert:
    - id: web-ui-plugin-manager
      name: '@linxin666/dsh-client-ui-plugin-manager'

# from ../dsh-community-plugins
- insert:
    - id: web-ui-community-plugins
      name: '@linxin666/dsh-client-ui-community-plugins'

# from ../dsh-market
- insert:
    - id: web-ui-market
      name: '@linxin666/dsh-client-ui-market'

# from ../dsh-task-board
- insert:
    - id: web-ui-task-board
      name: '@linxin666/dsh-client-ui-task-board'

# from ../dsh-git-graph
- insert:
    - id: web-ui-git-graph
      name: '@linxin666/dsh-client-ui-git-graph'

# from ../dsh-perf
- insert:
    - id: web-ui-dsh-perf
      name: '@linxin666/dsh-perf'
      config:
        enabled: true
        meterIntervalMs: 2000
        statsWindowSeconds: 120
        maxActiveSessions: 5
        maxEventsPerSec: 300

# from ../dsh-perf (patch row session-persistence-jsonl)
- id: session-persistence-jsonl
  name: '@deepseek-ai/dsh-session-persistence-jsonl'
  config:
    root: !!js dshHomePath('sessions')
    packChunks: true
    compression: zstd
    preparedSessionCacheSize: 5
    writeBatchMaxDelayMs: 500

# from ../dsh-remote-web-ui
- insert:
    - id: web-ui-remote-web-ui
      name: '@linxin666/dsh-remote-web-ui'

# from ../dsh-pet
- insert:
    - id: web-ui-pet
      name: '@linxin666/dsh-pet'

# from ../dsh-ssh
- insert:
    - id: web-ui-ssh
      name: '@linxin666/dsh-ssh'

# from ../dsh-tool-describe-image
- insert:
    - id: web-ui-describe-image
      name: '@linxin666/dsh-tool-describe-image'

# from ../dsh-liangshen
- insert:
    - id: web-ui-liangshen
      name: '@linxin666/dsh-liangshen'

# from ../dsh-skill-explorer
- insert:
    - id: web-ui-skill-explorer
      name: '@linxin666/dsh-client-ui-skill-explorer'

# from ../dsh-desktop-launcher
- insert:
    - id: web-ui-desktop-launcher
      name: '@linxin666/dsh-desktop-launcher'

# from ../dsh-doctor
- insert:
    - id: web-ui-doctor
      name: '@linxin666/dsh-doctor'

# from ../dsh-usage
- insert:
    - id: web-ui-usage
      name: '@linxin666/dsh-usage'

# from ../skins/skin-center
- insert:
    - id: web-ui-skin-center
      name: '@linxin666/dsh-client-ui-skin-center'

# from ../dsh-i18n
- insert:
    - id: web-ui-i18n
      name: '@linxin666/dsh-i18n'

# external bundle: dsh-better-sidebar

# from external bundle dsh-better-sidebar (insert row web-ui-better-sidebar)
- insert:
    - id: web-ui-better-sidebar
      name: 'dsh-better-sidebar'

# external bundle: @morlay/better-session

# from external bundle @morlay/better-session (insert row web-ui-session-branch)
- insert:
    - id: web-ui-session-branch
      name: '@morlay/session-branch'

# from external bundle @morlay/better-session (insert row web-ui-session-rdb)
- insert:
    - id: web-ui-session-rdb
      name: '@morlay/session-rdb'
      config:
        type: sqlite
        path: !!js dshHomePath('sessions', 'sessions.sqlite')

# from external bundle @morlay/better-session (insert row web-ui-conversation-message-actions)
- insert:
    - id: web-ui-conversation-message-actions
      name: '@morlay/ui-conversation-message-actions'

# inactive by default: the rows above ship disabled, so the stock persistence
# backend keeps serving sessions until you opt in. Enable with profile-level
# `disabled: false` overrides (or run scripts/dsh-better-session.mjs enable).
- id: web-ui-session-branch
  disabled: true
- id: web-ui-session-rdb
  disabled: true
- id: web-ui-conversation-message-actions
  disabled: true
